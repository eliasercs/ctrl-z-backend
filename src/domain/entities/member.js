export default class Member {
    constructor({ id, username, avatarUrl, memberCount}) {
        this.id = id
        this.username = username
        this.avatarUrl = avatarUrl
        this.memberCount = memberCount
    }
}